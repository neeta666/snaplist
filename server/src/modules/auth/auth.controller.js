// Auth module — controller placeholder.
//
// Per the Technical Design Document (section 4), controllers parse the
// request, call the appropriate service method, and shape the HTTP response.
// They must never contain business logic or talk to the database/providers
// directly — that stays in auth.service.js. No handlers are implemented yet;
// this file establishes where they will live (Slice 1).

// Placeholder export so other Slice 0 files can already reference this
// module's shape without importing something that doesn't exist yet.
export const authController = {
  // register(req, res, next) {}
  // login(req, res, next) {}
  // getCurrentUser(req, res, next) {}
  // logout(req, res, next) {}
};
