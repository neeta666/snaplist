// New Listing page — placeholder.
// The Upload -> AI Generate -> Edit -> Save flow is implemented across
// Slices 2 through 4, per the Development Checklist.

export default function NewListing() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-gray-900">New Listing</h1>
      <p className="text-sm text-gray-500 mt-1">Coming in Slices 2 through 4.</p>
    </div>
  );
}
