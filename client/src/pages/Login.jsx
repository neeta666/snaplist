// Login page — placeholder.
// Real form, validation, and submit handling are implemented in Slice 1
// (Authentication), per the Development Checklist.

export default function Login() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-gray-900">Log In</h1>
      <p className="text-sm text-gray-500 mt-1">Coming in Slice 1.</p>
    </div>
  );
}
