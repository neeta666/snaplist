// Dashboard page — placeholder.
// Summary cards, chart, search/filter/sort are implemented in Slice 6,
// per the Development Checklist. This route exists now purely so the
// routing skeleton has somewhere to land after login.

export default function Dashboard() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-gray-900">Dashboard</h1>
      <p className="text-sm text-gray-500 mt-1">Coming in Slices 4 and 6.</p>
    </div>
  );
}
